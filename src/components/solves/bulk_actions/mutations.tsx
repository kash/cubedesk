// This file is no longer needed as we're using tRPC hooks directly in the components
// Keeping it as a placeholder to make it easier to migrate without changing imports
// The actual mutations are now handled through tRPC API